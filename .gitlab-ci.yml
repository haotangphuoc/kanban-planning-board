image: maven:latest

stages:
  - build
  - test

build:
  stage: build
  script:
    - cd Backend-Trello
    - mvn clean compile

test:
  stage: test
  script:
    - cd Backend-Trello
    - mvn clean test
